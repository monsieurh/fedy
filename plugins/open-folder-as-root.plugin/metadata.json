{
	"icon": "settings",
	"label": "Open folder as root",
	"description": "Add 'open as root' option to context menu",
	"license": "Free",
	"category": "Utilities",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root rm /usr/share/contractor/open-folder-as-root.contract"
		},
		"status": {
			"command": "[ -f /usr/share/contractor/open-folder-as-root.contract ] && echo \"'Open as root' is installed.\" || echo \"'Open as root' is not installed.\""
		}
	}
}
